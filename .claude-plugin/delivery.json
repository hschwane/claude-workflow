{
  "$comment": "Every path this plugin delivers, and who owns it. Anything NOT listed here is the project's and is never touched. The update only ever touches a file the plugin actually changed between the installed and the target version.",
  "classes": {
    "project": "Created from a template once, then handed to the project. An update never rewrites it; when the upstream template changed, the update SUGGESTS how to fold that change in and lets the project decide. In practice these are rarely touched.",
    "plugin": "Belongs to the plugin and is replaced on update. `project-specific: <id>` marker blocks inside such a file protect project content across the replacement.",
    "mixed": "A plugin file where markers cannot work \u2014 the project's content and the plugin's are interleaved, or the format carries no comments. Claude merges these by hand, every time, always showing the diff."
  },
  "entries": [
    {
      "path": ".claude/skills/",
      "class": "plugin",
      "source": "skills/"
    },
    {
      "path": ".claude/agents/",
      "class": "plugin",
      "source": "agents/"
    },
    {
      "path": ".claude/guidelines/",
      "class": "plugin",
      "note": "Full mirror: every `.md` in the library lands in every project, refreshed on each update. There is no per-project selection \u2014 relevance is decided per task by `INDEX.md`'s triggers, not per project at install time. `LIBRARY.md` and `INDEX.md.template` are plugin-side only and are never copied in. A user-global guideline installed from `~/.claude/guidelines/` is project-side: keep it and its INDEX row.",
      "source": "templates/guidelines/",
      "sourceBefore": {
        "3.0.0": "templates/preferences/"
      }
    },
    {
      "path": ".claude/guidelines/INDEX.md",
      "class": "plugin",
      "note": "Copied verbatim. It ships a row for every guideline in the library and is identical in every project, so nothing is generated. Do NOT rebuild it row-by-row from the directory \u2014 that was needed when the installed set varied per project and now only loses rows. The one thing to preserve across a copy is a row pointing at a file outside the library: that is a user-global guideline, so re-append it.",
      "source": "templates/guidelines/INDEX.md.template",
      "sourceBefore": {
        "3.0.0": "templates/preferences/INDEX.md.template"
      }
    },
    {
      "path": ".claude/guidelines/README.md",
      "class": "plugin",
      "source": "templates/guidelines/README.md",
      "sourceBefore": {
        "3.0.0": "templates/preferences/README.md"
      }
    },
    {
      "path": ".claude/hooks/*.sh",
      "class": "plugin",
      "note": "Plugin-owned. A project adds its own hook as a new file \u2014 manifest-driven mirroring leaves it alone, and its settings.json entry survives because that merge only ever adds.",
      "source": "templates/hooks/"
    },
    {
      "path": ".claude/memory/.gitignore",
      "class": "plugin",
      "source": "templates/memory/.gitignore"
    },
    {
      "path": ".claude/memory/decisions.md",
      "class": "project",
      "note": "Created from a template with a topic index in its head, so a reader can load only the head and stop there on a miss.",
      "source": "templates/memory/decisions.md.template"
    },
    {
      "path": ".claude/memory/gotchas.md",
      "class": "project",
      "note": "Created from a template with a topic index in its head, so a reader can load only the head and stop there on a miss.",
      "source": "templates/memory/gotchas.md.template"
    },
    {
      "path": ".claude/memory/tech-debt.md",
      "class": "project",
      "note": "Created from a template with a topic index in its head, so a reader can load only the head and stop there on a miss.",
      "source": "templates/memory/tech-debt.md.template"
    },
    {
      "path": ".claude/memory/local-settings.md",
      "class": "project",
      "note": "gitignored; personal toggles"
    },
    {
      "path": ".claude/memory/context-*.md",
      "class": "project",
      "note": "gitignored; runtime state"
    },
    {
      "path": ".claude/settings.json",
      "class": "mixed",
      "merge": "Add missing hook entries and permissions.allow members; never remove; add statusLine only if absent; leave every other key.",
      "source": "templates/hooks/hooks.json"
    },
    {
      "path": ".claude/workflow-source.json",
      "class": "plugin",
      "note": "rewritten by the update itself"
    },
    {
      "path": "CLAUDE.md",
      "class": "plugin",
      "markerSyntax": "html",
      "blocks": [
        "identity",
        "ci-model"
      ],
      "keyMerge": [
        "workflow-settings"
      ],
      "note": "Plugin-owned; the marked project blocks survive replacement.",
      "source": "templates/CLAUDE.md.template"
    },
    {
      "path": "CONTRIBUTING.md",
      "class": "plugin",
      "markerSyntax": "html",
      "blocks": [
        "contributing",
        "ci-note"
      ],
      "note": "Plugin-owned; the marked project blocks survive replacement.",
      "source": "templates/CONTRIBUTING.md.template"
    },
    {
      "path": "README.md",
      "class": "project",
      "source": "templates/README.md.template"
    },
    {
      "path": "CHANGELOG.md",
      "class": "project",
      "source": "templates/CHANGELOG.md.template"
    },
    {
      "path": "src/CLAUDE.md",
      "class": "project",
      "source": "templates/src-claude.md.template"
    },
    {
      "path": "tests/CLAUDE.md",
      "class": "project",
      "source": "templates/tests-claude.md.template",
      "note": "The project's test-directory guide. `/project-init` creates it at `tests/CLAUDE.md`; an onboarded project may hold it at `test/`, `spec/` or wherever its suite lives \u2014 match the directory that exists rather than this literal path."
    },
    {
      "path": "docs/dev/code-style.md",
      "class": "plugin",
      "source": "templates/dev/code-style.md.template"
    },
    {
      "path": "docs/dev/architecture.md",
      "class": "project",
      "source": "templates/dev/architecture.md.template"
    },
    {
      "path": "docs/dev/setup.md",
      "class": "project",
      "source": "templates/dev/setup.md.template"
    },
    {
      "path": "docs/dev/deploy.md",
      "class": "project",
      "source": "templates/dev/deploy.md.template"
    },
    {
      "path": "docs/user/",
      "class": "project"
    },
    {
      "path": "docs/specs/",
      "class": "project"
    },
    {
      "path": "docs/VISION.md",
      "class": "project",
      "source": "templates/vision.md.template"
    },
    {
      "path": "scripts/ci.sh",
      "class": "mixed",
      "note": "The project's commands and the plugin's scaffolding are interleaved; replacing them would silently produce a gate that exits 0 with nothing to run.",
      "source": "templates/scripts/ci.sh"
    },
    {
      "path": "scripts/release.sh",
      "class": "mixed",
      "note": "The project's commands and the plugin's scaffolding are interleaved; replacing them would silently produce a gate that exits 0 with nothing to run.",
      "source": "templates/scripts/release.sh"
    },
    {
      "path": "scripts/claude-loop.sh",
      "class": "plugin",
      "source": "templates/scripts/claude-loop.sh"
    },
    {
      "path": ".github/workflows/ci.yml",
      "class": "mixed",
      "source": "templates/github/"
    },
    {
      "path": ".github/workflows/release.yml",
      "class": "mixed",
      "source": "templates/github/"
    },
    {
      "path": ".github/dependabot.yml",
      "class": "mixed",
      "source": "templates/github/dependabot.yml",
      "note": "The scaffolder uncomments the ecosystem matching this project's language, so the delivered file carries per-project state that a wholesale replace would revert \u2014 merge it and keep the project's enabled ecosystems."
    },
    {
      "path": "railway.json",
      "class": "project",
      "note": "Delivered once at init; the project owns it afterwards. An update only suggests upstream changes.",
      "source": "templates/configs/railway.json"
    },
    {
      "path": "tsconfig*.json",
      "class": "project",
      "note": "Delivered once at init; the project owns it afterwards. An update only suggests upstream changes.",
      "source": "templates/configs/"
    },
    {
      "path": "eslint.config.js",
      "class": "project",
      "note": "Delivered once at init; the project owns it afterwards. An update only suggests upstream changes.",
      "source": "templates/configs/eslint.config.js"
    },
    {
      "path": ".prettierrc",
      "class": "project",
      "note": "Delivered once at init; the project owns it afterwards. An update only suggests upstream changes.",
      "source": "templates/configs/.prettierrc"
    },
    {
      "path": ".gitignore",
      "class": "project",
      "source": "templates/gitignore/"
    },
    {
      "path": ".claude/ui/",
      "class": "plugin",
      "note": "UI templates a guideline references; copied so the reference resolves inside the project. Install a file only when the guideline that references it is installed (`changelog-template.html` \u2190 `changelog.md`).",
      "source": "templates/ui/"
    },
    {
      "path": "docs/specs/spec.md.template",
      "class": "plugin",
      "note": "Read by /draft and /plan to create every ticket.",
      "source": "templates/spec.md.template"
    },
    {
      "path": ".env.example",
      "class": "project",
      "note": "Referenced by docs/dev/setup.md; created at init, owned by the project afterwards."
    },
    {
      "path": "LICENSE",
      "class": "project",
      "source": "templates/LICENSE-MIT.template"
    },
    {
      "path": "docs/changelog/",
      "class": "project",
      "note": "Created by /release on the first split, not at init."
    },
    {
      "path": ".github/ISSUE_TEMPLATE/",
      "class": "project",
      "note": "Created at init/onboard when GitHub integration is on; the project edits them freely.",
      "source": "templates/github/"
    },
    {
      "path": "package.json",
      "class": "project",
      "source": "templates/configs/package.json.template",
      "note": "Also the default `version-source`. The plugin seeds it once; every later change is the project's."
    },
    {
      "path": "pyproject.toml",
      "class": "project",
      "source": "templates/configs/pyproject.toml"
    },
    {
      "path": "CMakeLists.txt",
      "class": "project",
      "source": "templates/configs/CMakeLists.txt"
    },
    {
      "path": "mkdocs.yml",
      "class": "project",
      "source": "templates/configs/mkdocs.yml.template"
    },
    {
      "path": "scripts/generate-version.js",
      "class": "project",
      "source": "templates/configs/generate-version.js"
    },
    {
      "path": "src/version.h.in",
      "class": "project",
      "source": "templates/configs/version.h.in"
    },
    {
      "path": ".prettierignore",
      "class": "plugin",
      "source": "templates/configs/.prettierignore",
      "note": "Plugin scaffolding, not project content: it exists so `prettier --check .` does not fail on the ~40 plugin-owned files under `.claude/`. Formatting those instead is not a fix \u2014 the update replaces them. `plugin` class so an update installs it into a project that predates it. A project adding its own ignores should do it in `.prettierignore.local` or accept that this file is replaced. **TypeScript/JavaScript only** \u2014 a project with no prettier must not be given a prettier ignore-file, at init or on update."
    },
    {
      "path": "src/index.ts",
      "class": "project",
      "note": "Created once as a committed entry point. src/version.ts is gitignored, so without a tracked source file a clean clone has nothing to lint or compile and CI is red on the first push. The project owns it from then on."
    },
    {
      "path": "package-lock.json",
      "class": "project",
      "note": "Generated by `npm install` at init and committed \u2014 CI runs `npm ci`, which fails outright without it. Never hand-edited; `protect-files.sh` blocks writes to it."
    },
    {
      "path": "tests/",
      "class": "project",
      "note": "Created at init with tests/unit/ and tests/integration/ (the split scripts/ci.sh's fast and full modes rely on) plus .gitkeep files so the directories survive a clone. The project owns everything in it except tests/CLAUDE.md."
    },
    {
      "path": "src/version.ts",
      "class": "project",
      "note": "Generated by the build from the manifest version and gitignored, so it is absent from a fresh clone. scripts/ci.sh regenerates it in its GENERATE_SOURCES stage before lint and typecheck run \u2014 without that the gate passes locally and fails on the first push."
    },
    {
      "path": "Cargo.toml",
      "class": "project",
      "note": "Rust manifest and the default `version-source`. The scaffolder writes it inline (no template ships); every later change is the project's."
    },
    {
      "path": ".clang-format",
      "class": "project",
      "note": "C++ formatter config, written inline by the scaffolder. `scripts/ci.sh`'s format stage calls it."
    },
    {
      "path": "uv.lock",
      "class": "project",
      "note": "Python lockfile \u2014 generated at init and committed. CI installs `--locked`, which fails without it. Cargo.lock and package-lock.json are the same case for their languages."
    },
    {
      "path": "Cargo.lock",
      "class": "project",
      "note": "Rust lockfile \u2014 generated at init and committed. `cargo --locked` in CI fails without it."
    },
    {
      "path": "web/",
      "class": "project",
      "note": "An additional source root named by the architecture (a frontend, a worker). The scaffolder creates it, adds it to tsconfig's include, gives it a build step and commits an entry point \u2014 a root the gate cannot see is worse than one that does not exist. Everything in it is the project's."
    }
  ],
  "removedIn3": [
    {
      "path": "docs/workflow/README.md",
      "reason": "plugin procedure \u2014 lives in the skills; the human-facing version is CONTRIBUTING.md"
    },
    {
      "path": "docs/workflow/lifecycle.md",
      "reason": "same; 'one session per branch' moved to CLAUDE.md"
    },
    {
      "path": "docs/workflow/conventions.md",
      "reason": "same; commit and branch conventions are in CONTRIBUTING.md and code-style.md"
    },
    {
      "path": "docs/workflow/quality.md",
      "reason": "test strategy moved to docs/dev/code-style.md; the gate is described in CLAUDE.md and CONTRIBUTING.md"
    },
    {
      "path": "docs/workflow/release.md",
      "reason": "version source and branching are settings; steps live in /release; required secrets moved to docs/dev/deploy.md"
    },
    {
      "path": "docs/workflow/decisions.md",
      "reason": "replaced by the workflow-settings block in CLAUDE.md"
    },
    {
      "path": "docs/workflow/deploy.md",
      "reason": "moved to docs/dev/deploy.md \u2014 it is project operations, not workflow procedure"
    },
    {
      "path": "docs/dev/style-guide.md",
      "reason": "merged into docs/dev/code-style.md"
    },
    {
      "path": "docs/dev/adr/",
      "reason": "retired; lasting decisions go to .claude/memory/decisions.md, detail to docs/dev/"
    },
    {
      "path": ".claude/preferences/",
      "reason": "renamed to .claude/guidelines/"
    },
    {
      "path": ".claude/project-notes/",
      "reason": "retired; deviations to decisions.md, traps to gotchas.md"
    },
    {
      "path": ".claude/skills/workflow-decisions/",
      "reason": "renamed to workflow-settings; the old skill writes to docs/workflow/decisions.md, which v3 deletes"
    }
  ],
  "rules": [
    "Not listed \u2192 project. Never touched.",
    "Before creating a new file, check whether the project already has one by that name; if so, resolve it explicitly instead of overwriting.",
    "Only files whose plugin version actually changed between the installed and target release are touched at all.",
    "Mirroring a plugin directory refreshes the listed files and deletes what a previous manifest listed and this one no longer does \u2014 anything else in that directory belongs to the project and stays.",
    "Removing or moving a file: inspect what project content it holds first and resolve that case specifically. Never delete a path without accounting for the project's share of it.",
    "A path in this manifest is the path IN A PROJECT. `source` is where it comes from in the plugin repo \u2014 always diff `git show v{current}:{source}` against the clone's `{source}`, never against the project path.",
    "A `plugin`-class path the new manifest lists and the project does not have is INSTALLED, not skipped. 'Only changed files are touched' is about files that exist on both sides.",
    "A `source` file ending in `.template` is delivered with that suffix stripped, unless the manifest `path` itself ends in `.template` (only `docs/specs/spec.md.template`, which the project keeps as a template). So `templates/guidelines/INDEX.md.template` lands as `.claude/guidelines/INDEX.md`.",
    "The most specific path wins. `docs/specs/spec.md.template` is `plugin` even though `docs/specs/` is `project`; a directory entry never overrides a file entry inside it.",
    "An entry with no `source` is project state the plugin does not author (`local-settings.md`, `context-*.md`, `docs/user/`, `LICENSE`, \u2026). It is never diffed and never written by an update.",
    "Where `source` is a directory but `path` is a single file, the plugin ships language/target variants and the project holds exactly one. Resolve the member from `variants` in `workflow-source.json`; if that is missing, detect it from the project's own file and record it. Never diff the whole directory.",
    "`sourceBefore` records where an entry's source lived in earlier plugin versions. Use it when the installed version is STRICTLY BELOW the version it lists; at or above it the current `source` is correct. `git show v{current}:{source}` fails outright when the plugin renamed its own directory, and a failed OLD must never be read as 'new file, safe to overwrite'."
  ]
}
