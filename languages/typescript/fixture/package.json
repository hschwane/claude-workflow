{
  "name": "fixture",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests --dir tests/unit",
    "test:changed": "vitest related --run --passWithNoTests --dir tests/unit",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "prettier": "^3.2.0"
  }
}
