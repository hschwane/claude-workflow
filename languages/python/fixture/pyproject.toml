[project]
name = "fixture"
version = "0.1.0"
requires-python = ">=3.11"

[dependency-groups]
dev = ["pytest>=8", "ruff>=0.4"]

[tool.ruff]
extend-exclude = [".claude/"]

[tool.pytest.ini_options]
pythonpath = ["src"]
