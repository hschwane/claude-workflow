---
id: {{TYPE}}-{{NNN}}
type: {{feature|bug}}
status: draft            # draft → ready (after /plan) → in-progress (/implement) → done
version: ~
test_scope: ~           # unit | +integration | +e2e — set by /plan, within the project default
created: {{YYYY-MM-DD}}
updated: {{YYYY-MM-DD}}
github_issue: ~
---

# {{Title}}

## Goal
As a [user], I want [goal], so that [benefit].

<!-- Bug format:
**Observed:** [what happens]   **Expected:** [what should happen]
**Steps to reproduce:** 1. …  2. …
-->

## Acceptance Criteria
<!-- OBSERVABLE — an action → a checkable result. This is the contract /verify checks against.
     Plain bullets, NOT checkboxes: nothing in the workflow ticks these, so a checkbox here
     leaves every completed spec looking half-done and inflates the subtask count the Stop
     hook reports. /verify maps each one to evidence in its report instead. -->
- Running/calling X → produces observable result Z
- Invalid input W → rejected with error/message V

## Approach & Interfaces
<!-- Enough to implement without re-deciding architecture mid-build. The key signatures to add/change. -->
```
// interfaces / signatures
```

<!-- Applied guidelines: <.claude/guidelines/x.md, …>   (added by /plan when a guideline matched; treated as
     recommendations — note anything adapted or deliberately rejected + why, not just a bare file list;
     /implement re-reads these) -->


## Documentation impact
<!-- One or two lines, decided by /plan — NOT a checklist, and "None." is a complete answer.
     Are docs affected, and which; or is there a new algorithm, interface, file format,
     protocol, contract or other concept that someone would otherwise have to reconstruct by
     reading the whole implementation? That last kind belongs in docs/dev/ with a row in
     docs/dev/README.md. Decided here, at planning time, because at implementation time it is
     the first thing dropped under pressure — and /verify checks against this line. -->
None.

## Subtasks
<!-- Ordered; each a green-committable unit. -->
- [ ] #1: [title] — [what]

## Smoke steps
<!-- Written by /verify: 1-3 for the main story as intended, one per acceptance criterion, plus
     any needed for interactions between criteria or areas this could plausibly affect.
     Each: exact action + literal inputs → exact observable expected result. Handed BLACKBOX to
     the smoke-tester (no spec, no code), which makes no judgement calls — anything left
     implicit is lost, and a step needing interpretation comes back as could-not-complete. -->

## Open Questions
<!-- Must be empty before status → ready. -->

## Criteria verification
<!-- Written by /verify: one row per acceptance criterion, four columns (criterion verbatim +
     spec line · literal expected · literal observed · where the observed value came from).
     The expected column comes from the criterion, never from the code or a run. A spec cannot
     reach status: done with this section missing or short a row — scripts/criteria-check.sh. -->
