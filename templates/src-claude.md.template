# Source — {{PROJECT_NAME}}

Loaded when you touch anything under `src/`, and not when planning, drafting or answering questions about the project.

**Coding rules are not here.** Style, naming, error handling, comments, security, design and the language specifics live in [`docs/dev/code-style.md`](../docs/dev/code-style.md) — one source, maintained by the workflow. Read it before writing code here. This file holds only what is true of *this* codebase.

## Layout

```
src/
{{SRC_STRUCTURE}}
```

## Key types

The interfaces everything else is built on live in `{{TYPES_FILE}}`. Read them before adding new code — most "how do I represent this?" questions are already answered there.

## Patterns in this codebase

- {{KEY_PATTERN_1}}
- {{KEY_PATTERN_2}}

<!-- Add what a newcomer would otherwise have to infer from reading around:
     which module owns what, how errors travel, where configuration is resolved,
     a convention that is deliberate rather than accidental.
     A rule that departs from code-style.md belongs in .claude/memory/decisions.md,
     dated and with a reason — not silently here. -->
