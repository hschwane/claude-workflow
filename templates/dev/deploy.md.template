# Deploy Process: {{PROJECT_NAME}}

## Environments
<!-- One row per environment that exists. `healthcheck.sh --env <name>` reads its URL from here,
     and deploy-reference.sh compares the two targets to refuse deploying develop to production.
     Delete the reference row under main-only, or where the project chose not to have one. -->

| Environment | URL | Fed by | Data | Secrets |
|---|---|---|---|---|
| production | {{HEALTH_CHECK_URL}} | `/release` | real | {{DEPLOY_SECRET_NAME}} |
| reference | {{REFERENCE_URL}} | `develop`, via `scripts/deploy-reference.sh` | its own — never production's | its own |
| dev | local, via `scripts/dev.sh` | nothing | test fixtures | test-only |

## Target Environment
{{DEPLOY_TARGET}}
<!-- Examples: Railway, Vercel, AWS EC2, Kubernetes, Fly.io, VPS via SSH -->
<!-- Railway is the preferred default. When deploying on Railway, the specifics
     (scale-to-zero, EU region, URL = project name, watch-path exclusions, and the
     rule that Railway-specific features live behind a project-defined interface for
     portability) live in the standing preference `.claude/guidelines/railway.md` —
     keep them there, not duplicated here. -->

## Platform Settings
{{PLATFORM_SETTINGS}}
<!-- Record region, scaling (scale-to-zero?), domain/URL, and other platform config here.
     On Railway, `railway.json` at the repo root pins these as config-as-code
     (see .claude/guidelines/railway.md). -->

<!-- Watch-path exceptions (if any): none — using default railway.json patterns -->



## Trigger
{{DEPLOY_TRIGGER}}
<!-- Examples:
- Automatic on release tag push (via release.yml)
- Manual via /release skill
- On merge to main
-->

## Deploy Steps

### Prerequisites
- Secret `{{DEPLOY_SECRET_NAME}}` must be set (a GitHub Actions secret where the release runs in CI; an environment variable on the deploy host otherwise)
- {{OTHER_PREREQUISITES}}

### Automated steps
{{AUTOMATED_DEPLOY_STEPS}}

### Manual Steps (if any)
{{MANUAL_DEPLOY_STEPS}}

## Rollback
{{ROLLBACK_PROCEDURE}}
<!-- This is the only safety net once a merge can ship. It must be a real, exercised procedure —
     an untested rollback is not a safety net. Exercise it once when the deploy pipeline is first
     stood up. And it protects users while the cause is diagnosed; it is not the resolution.
     A release left rolled back with no fix is an unfinished job. -->
<!-- Example:
1. git revert to previous tag: git checkout v1.1.0
2. Re-run deploy steps with old version
3. OR: use platform UI to roll back deployment
-->

## Health Check
After deploy, verify:
- [ ] {{HEALTH_CHECK_URL}} returns 200
- [ ] {{SMOKE_TEST_DESCRIPTION}}

## Required deployment secrets
| Secret | Description |
|--------|-------------|
| `{{SECRET_NAME}}` | {{SECRET_DESCRIPTION}} |
