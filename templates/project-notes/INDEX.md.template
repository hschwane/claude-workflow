# Project notes index

Trigger → file map for `.claude/project-notes/` — this project's own standing rules. Skills read THIS file (not the note bodies); a body is read only when its trigger matches. Keep each row to one concrete line.

Never modified by `/workflow-update`. Workflow-preference triggers live in `.claude/preferences/INDEX.md`.

<!-- Append rows at the end of this file so the table stays contiguous, e.g.:
     | The billing module, invoices, dunning | .claude/project-notes/billing.md |
     | Database migrations, schema changes   | .claude/project-notes/migrations.md | -->

| When the task involves… | Read |
|---|---|
