# {{PROJECT_NAME}}

{{PROJECT_DESCRIPTION}}

## Quick Reference

| Command | Description |
|---------|-------------|
| `/draft feature "title"` | Add raw feature idea to backlog |
| `/draft bug "title"` | Report a bug |
| `/brainstorm` | Analyze project state + generate backlog ideas |
| `/prioritize` | Rank backlog against vision, pick items for next version |
| `/refine FEAT-001` | Refine a draft into a ready spec (RE + Tech Planner) |
| `/implement FEAT-001` | Implement a ready spec (tests first, then code) |
| `/commit` | Quality-gated conventional commit |
| `/pr` | Create PR, wait for CI, run reviews, auto-merge |
| `/release patch\|minor\|major` | Bump version, tag, trigger CI release |
| `/workflow-update` | Update claude-workflow plugin to latest version |
| `/resume` | Resume in-progress work after token limit or interruption |
| `/unsupervised on\|off` | Toggle unsupervised mode (no questions, loop-safe) |

## Architecture

{{ARCHITECTURE_SUMMARY}}

Tech stack: {{TECH_STACK}}

Details (read on demand, not auto-loaded):
- `docs/workflow/README.md` — workflow overview
- `docs/dev/architecture.md` — architecture details

## Agents — delegate proactively

IMPORTANT: Use these subagents automatically whenever they fit — do not ask first. The main context is the scarcest resource; anything that requires reading many files or produces large output belongs in a subagent.

| Agent | Use when |
|-------|----------|
| `code-explorer` | Any codebase question that needs reading more than 3-4 files; before implementing in unfamiliar code |
| `test-runner` | Any full test/lint run — returns a condensed failure report instead of raw output |
| `workflow-coach` | The user asks how the dev workflow works or which command to use |
| `product-owner` | Judging ideas/backlog items against the vision (used by /brainstorm, /prioritize) |
| `code-reviewer` / `security-reviewer` / `architect-reviewer` | Reviewing a diff (used by /pr; also on direct request) |
| `requirements-engineer` / `tech-planner` | Structured refinement (used by /refine) |
| `test-writer` | Spec-only test creation (used by /implement Phase 1) |
| `documentation-writer` | Doc updates after implementation (used by /implement) |

Likewise, use the workflow skills (`/commit`, `/draft`, `/resume`) whenever they match the task instead of ad-hoc equivalents.

Model routing: agents pin their own model (`haiku` for mechanical work, `sonnet` for execution); planning/review agents inherit the session model. `/refine` and `/pr` ask once (supervised mode only) which tier their inherit-agents should use — session model / better-than-Sonnet (`opus`) / `sonnet` / `haiku` — and pass it as the per-invocation `model` parameter. Adjust defaults per agent in `.claude/agents/{name}.md`.

For pure "where is X?" file/symbol discovery, use the **built-in Explore agent** (Haiku) — `code-explorer` is for briefings that need judgment (patterns, pitfalls, interfaces).

## Session Behavior

If the session start context contains `=== AUTO-RESUME REQUIRED ===`, immediately execute `/resume` without waiting for user input.

If a hook reports `SUPERVISED_USAGE_ALERT`: use `AskUserQuestion` to ask the user whether they want to switch to unsupervised mode now (so auto-resume becomes available). Options: yes at 80% / yes with a custom threshold / no. If they agree, run `/unsupervised on [threshold]`; if not, continue normally. The alert will not repeat until usage drops and rises again.

If `.claude/memory/settings.md` contains `unsupervised: true`:
- **Never** use `AskUserQuestion` — apply autonomous defaults from the `/unsupervised` skill
- If a hook reports `USAGE THRESHOLD REACHED`: commit the current atomic step, update the checkpoint, then run `bash .claude/hooks/usage-guard.sh --wait` (3-minute Bash timeout) repeatedly until it prints `RESUME_OK`, then continue working
- If you hit a genuine blocker (merge conflict, missing credentials, unresolvable ambiguity), write `## Blocked: <reason>` to `.claude/memory/context.md` and stop — do not wait for input
- When all work is complete, clear `## In Progress` from `.claude/memory/context.md` and stop

## Memory

`.claude/memory/` contains project memory (decisions, context, gotchas, tech-debt). Read these when they are relevant — they are not auto-loaded.

## Context Management

Compress the conversation proactively to avoid token exhaustion mid-task. Long sessions accumulate context fast — a single `/implement` run for a non-trivial feature can approach the limit.

**Compact after every natural boundary:**
- After each subtask's commit during `/implement` (right before starting the next subtask)
- After a `/pr` review-and-fix cycle completes
- After `/refine` finishes and the spec is ready
- Whenever the conversation exceeds ~40 messages or starts feeling sluggish

**Before every compact, ensure all state is on disk:**
1. All in-progress changes are committed and pushed
2. `.claude/memory/context.md` reflects the current task ID, branch, and next step
3. Spec checkboxes match actual progress (they are the subtask source of truth)

**How to compact:** run `/compact` in Claude Code. The harness summarizes the conversation; the summary plus the memory files give full orientation for the next burst of work.

**After compacting:** read `.claude/memory/context.md` before continuing so you pick up exactly where the work left off.

**Safe vs. unsafe compact windows:**
- **Safe**: immediately after a successful commit + checkpoint update — everything is persisted
- **Unsafe**: mid-subtask with uncommitted edits, or while waiting for a tool result — finish the atomic unit first
